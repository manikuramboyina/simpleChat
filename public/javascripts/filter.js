/**
 * Created by Mani on 12/10/2014.
 */

(function () {
    "use strict";

    /** chatModel service, provides chat rooms (could as well be loaded from server) */
    angular.module('sseChat.filters', ['sseChat.controllers']).filter('filterUsers', function () {
            return function (friends) {


            }
        }
    )


})();